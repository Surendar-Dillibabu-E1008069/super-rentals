<button type="button" class="image{{if this.isLarge " large"}}" onclick={{action 'toggleSize'}}>
  <img src={{src}} alt={{alt}}>
  <small>View {{if this.isLarge "Smaller" "Larger"}}</small>
</button>
